on:
  push:
    paths-ignore: # don't run the fast version when an Rmd file changes
      - "**.Rmd"
      - "environment.yml"
      - ".github/workflows/ci.yaml"

name: ci_fast

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          # submodules: true  # Fetch Hugo themes (true OR recursive)
          fetch-depth: 3 # Fetch all history for .GitInfo and .Lastmod

      - uses: actions/setup-node@v1
        with:
          node-version: "12"
      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - run: npm ci
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build

      - uses: actions/upload-artifact@v2
        with:
          name: website
          path: ./public
          
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.DELPHI_ECR_ACCESS_ID }}
          aws-secret-access-key: ${{ secrets.DELPHI_ECR_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: cmu-delphi/www-main
        run: |
          case "${GITHUB_REF#*/}" in
          main)
            awsTag="latest"
            ;;
          *)
            awsTag="${GITHUB_REF#*/}" # replace `/` with `_` in branch name
            ;;
          esac
          echo "using tag: --${awsTag}--"
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$awsTag --file ./devops/web/Dockerfile .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$awsTag